// CURSOR

// var img = document.querySelector(".cursor");

// document.addEventListener("mousemove", updateImage);

// function updateImage(event) {
//     img.style.transform = `translate(${event.pageX}px, ${event.pageY}px)`;
// }
